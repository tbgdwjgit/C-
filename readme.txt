C# 是大小写敏感的。
所有的语句和表达式必须以分号（;）结尾。
未赋值变量不可以使用
程序的执行从 Main 方法开始。
与 Java 不同的是，文件名可以不同于类的名称。

多行注释以 /* 开始，并以字符 */ 终止
单行注释是用 '//' 符号



Console.WriteLine("Hello World!");

获取GUID: Guid id = Guid.NewGuid();


1.方法：
  C#不支持全局方法。
  必须显式指定方法参数类型和返回类型，不能用var,没有返回值用void。
  提供默认值，就是可选参数，放在最后。